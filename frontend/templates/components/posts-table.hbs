<div>
<h1>{{i18n 'Posts'}}</h1>
<div id="posts-table">
  {{#if posts.length}}
    <table border="1">
      <thead>
      <tr>
        <th>{{i18n 'Nombre'}}</th>
        <th>{{i18n 'Usuario'}}</th>
        <th>{{i18n 'Actions'}}</th>
      </tr>
      </thead>
      <tbody>
      {{#each posts}}
        <tr fronty-component="PostRowComponent" key="{{IdNota}}" model="posts[{{@index}}]" id="item-{{IdNota}}"></tr>
      {{/each}}
    </tbody>
    </table>
  {{/if}}
</div>
{{#if user.isLogged}}
  <a href='#add-post'>{{i18n 'Add post'}}</a>
{{/if}}
</div>
